"""M14 · Executor ccxt — CHỈ mở sau khi vượt đủ GATE 1–4 (MASTER_PLAN §7).

Mặc định TẮT. `TRADING_ENABLED=false` và `TRADING_MODE=paper` trong .env.
Mọi lần khởi động lại đều quay về trạng thái an toàn — đó là thiết kế, không
phải phiền toái.
"""
